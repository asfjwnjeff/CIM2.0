# 风控审批模块数据修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复风控审批模块的四类数据问题：数据库编码、ID 引用渲染、前后端字段对齐、API/Store 同步

**Architecture:** 从数据底层到 UI 展示层逐层修复。先修数据库编码和 Schema（防止新数据损坏），再对齐 TypeScript 类型（消除幽灵字段），接着补齐 API 和 Store 同步（防止数据丢失），最后修复 UI 层的 ID 渲染问题（改善用户体验）

**Tech Stack:** Next.js 14 App Router, TypeScript, Drizzle ORM, SQLite (sql.js), Zustand, Tailwind CSS

---

## 文件结构与职责

| 文件 | 职责 | 本次改动 |
|------|------|---------|
| `src/db/index.ts` | 数据库连接 + WASM 加载 | 添加 UTF-8 PRAGMA |
| `src/db/schema.ts` | Drizzle ORM 表定义 | 补充缺失列 |
| `src/lib/types.ts` | 全部 TypeScript 类型 | 清理幽灵字段，统一命名 |
| `src/lib/utils.ts` | 通用工具函数 | 新增 ID→名称解析函数 |
| `src/lib/store.tsx` | Zustand 全局状态 | 补齐 API 同步调用 |
| `src/lib/sample-data.ts` | 初始种子数据 | 修正乱码、适配类型 |
| `src/app/api/risk-approvals/route.ts` | 风控审批 CRUD API | 修复 parseRecord |
| `src/app/api/approval-workflows/route.ts` | 审批流 API | 新增 POST/PUT/DELETE |
| `src/app/api/auto-approval-rules/route.ts` | 自动规则 API | 新增 POST/PUT/DELETE |
| `src/app/api/approval-fields/route.ts` | 字段配置 API | 部分更新保护 |
| `src/app/approvals/page.tsx` | 审批列表页 | 乱码修正、ID 渲染 |
| `src/app/approvals/new/page.tsx` | 审批新建页 | 适配类型变更 |
| `src/app/approvals/[id]/page.tsx` | 审批详情页 | 乱码修正、ID 渲染 |
| `src/app/approvals/[id]/edit/page.tsx` | 审批编辑页 | ID 渲染修复、类型适配 |
| `src/app/approval/workflows/page.tsx` | 流程配置列表 | 统一 isTradeAgency |
| `src/app/approval/workflows/new/page.tsx` | 流程配置编辑 | 统一 isTradeAgency |
| `src/app/approval/workflows/[id]/page.tsx` | 流程配置详情 | 统一 isTradeAgency |

---

### Task 1: 数据库编码修复

**Files:**
- Modify: `src/db/index.ts`
- Modify: `src/lib/sample-data.ts`

- [ ] **Step 1: 在数据库初始化时添加 UTF-8 PRAGMA**

在 `src/db/index.ts` 的 `getDb()` 函数中，创建 SQL.Database 后立即执行 PRAGMA：

```typescript
// 在 getDb() 函数中，sqlDb = new SQL.Database(buffer) 或 new SQL.Database() 之后添加：
sqlDb.run('PRAGMA encoding = "UTF-8"');
```

修改后 `getDb()` 函数的关键部分：

```typescript
export async function getDb() {
  if (dbInstance) return dbInstance;

  const SQL = await initSqlJs({
    locateFile: () => findWasmFile(),
  });

  if (fs.existsSync(DB_PATH)) {
    const buffer = fs.readFileSync(DB_PATH);
    sqlDb = new SQL.Database(buffer);
  } else {
    sqlDb = new SQL.Database();
  }

  // 显式设置 UTF-8 编码，防止中文乱码
  sqlDb.run('PRAGMA encoding = "UTF-8"');

  dbInstance = drizzle(sqlDb, { schema });
  return dbInstance;
}
```

- [ ] **Step 2: 修正 sample-data.ts 中的乱码记录**

在 `src/lib/sample-data.ts` 中搜索包含 `�` 字符的公司名称并修正。已知需修正：

```
// 搜索模式：companyName 或 name 字段中包含 � 字符的记录
// 修正为正确的公司名称
```

- [ ] **Step 3: 验证 — 运行 TypeScript 编译**

```bash
cd project_20260526_111004/projects
npx tsc --noEmit
```

Expected: 零类型错误。

---

### Task 2: 前后端字段对齐 — 类型清理

**Files:**
- Modify: `src/lib/types.ts:535-718`

- [ ] **Step 1: 修复 `RiskApprovalStatus` 类型**

```typescript
// 修改前（types.ts 第 535 行）
export type RiskApprovalStatus = 'draft' | 'pending' | 'approved' | 'rejected';

// 修改后
export type RiskApprovalStatus = 'draft' | 'in_review' | 'approved' | 'rejected';
```

- [ ] **Step 2: 清理 `RiskApproval` 接口中的幽灵字段**

在 `src/lib/types.ts` 第 559-603 行的 `RiskApproval` 接口中：

```typescript
// 删除以下字段（数据库无对应列）：
// - customerId?: string;
// - customerName?: string;
// - approvalNumber?: string;
// - riskPurpose?: string;        (统一使用 riskControlPurpose)
// - approvalNodes?: ApprovalNode[];  (统一使用 approvalSteps)
```

修改后的 `RiskApproval` 接口头部：

```typescript
export interface RiskApproval {
  id: string;
  // 已删除: customerId, customerName, approvalNumber, riskPurpose, approvalNodes
  companyName?: string;
  englishName?: string;
  parentCompany?: string;
  subsidiaryCompany?: string;
  serviceProducts?: ServiceProduct[];
  serviceProduct?: string;
  isTradeAgent?: string;        // '是' | '否'
  businessType?: string;
  // ... 其余字段保持不变
```

- [ ] **Step 3: 清理 `ApprovalWorkflow` 接口中的重复拼写**

在 `src/lib/types.ts` 第 607-621 行的 `ApprovalWorkflow` 接口中：

```typescript
// 删除重复拼写
// - isTradeAgent?: boolean;  // 仅保留 isTradeAgency
// - updatedBy?: string;      // 数据库无此列
```

修改后：

```typescript
export interface ApprovalWorkflow {
  id: string;
  name: string;
  serviceProducts?: ServiceProduct[];
  serviceProduct?: string;
  isTradeAgency?: boolean;
  approvalNodes?: ApprovalNode[];
  status: Status;
  remark?: string;
  createdBy?: string;
  createdAt: string;
  updatedAt?: string;
}
```

- [ ] **Step 4: 清理 `AutoApprovalRule` 接口**

在 `src/lib/types.ts` 第 656-674 行的 `AutoApprovalRule` 接口中：

```typescript
// 删除：
// - serviceProducts?: ServiceProduct[];   (数据库存单值)
// - message?: string;                     (数据库无此列，可保留为前端派生字段)
```

`message` 字段保留（前端计算字段），仅删除 `serviceProducts` 数组。

- [ ] **Step 5: 验证编译**

```bash
cd project_20260526_111004/projects
npx tsc --noEmit
```

Expected: 会有类型错误（因为其他文件还在引用被删除的字段）。这些将在后续任务中逐一修复。

---

### Task 3: 前后端字段对齐 — Schema 补充

**Files:**
- Modify: `src/db/schema.ts`

- [ ] **Step 1: 在 `autoApprovalRules` 表中添加缺失列**

在 `src/db/schema.ts` 第 163-177 行的 `autoApprovalRules` 表定义中，在 `priority` 行之后添加：

```typescript
export const autoApprovalRules = sqliteTable('auto_approval_rules', {
  id: text('id').primaryKey(),
  name: text('name').notNull(),
  serviceProduct: text('service_product'),
  status: text('status').default('active'),
  conditionLogic: text('condition_logic').default('AND'),
  conditions: text('conditions'),  // JSON array
  actions: text('actions'),        // JSON array
  successMessage: text('success_message'),
  failureMessage: text('failure_message'),
  priority: integer('priority').default(99),
  // 新增列：
  remark: text('remark'),           // ← 新增
  isSystem: integer('is_system', { mode: 'boolean' }).default(false),  // ← 新增
  createdBy: text('created_by'),
  createdAt: text('created_at'),
  updatedAt: text('updated_at'),
});
```

- [ ] **Step 2: 验证编译**

```bash
cd project_20260526_111004/projects
npx tsc --noEmit
```

---

### Task 4: 前后端字段对齐 — 全局引用适配

**Files:**
- Modify: `src/lib/store.tsx`
- Modify: `src/lib/sample-data.ts`
- Modify: `src/app/approvals/new/page.tsx`
- Modify: `src/app/approvals/[id]/page.tsx`
- Modify: `src/app/approvals/[id]/edit/page.tsx`
- Modify: `src/app/approvals/page.tsx`
- Modify: `src/app/approval/workflows/page.tsx`
- Modify: `src/app/approval/workflows/new/page.tsx`
- Modify: `src/app/approval/workflows/[id]/page.tsx`
- Modify: `src/app/api/risk-approvals/route.ts`
- Modify: `src/app/api/auto-approval-rules/route.ts`

- [ ] **Step 1: 搜索并替换所有 `isTradeAgent` → `isTradeAgency`（类型/接口使用处）**

全局搜索 `isTradeAgent`（TypeScript 接口字段名使用处），替换为 `isTradeAgency`。注意：数据库列名 `is_trade_agent` 不变，仅统一前端类型中的拼写。

受影响文件：
- `src/app/approval/workflows/page.tsx` — workflow 对象属性访问
- `src/app/approval/workflows/new/page.tsx` — workflow 编辑表单
- `src/app/approval/workflows/[id]/page.tsx` — workflow 详情

（注：`isTradeAgent` 作为 `RiskApproval` 的字段名保留，因为它是独立概念 —— 审批记录中表示"是否贸易代理"）

- [ ] **Step 2: 搜索并替换所有 `isTradeAgent`（仅 `ApprovalWorkflow` 的拼写）**

在审批流模板相关的 3 个页面中，将所有引用 `workflow.isTradeAgent` 的地方改为 `workflow.isTradeAgency`。

- [ ] **Step 3: 搜索并替换所有引用已删除幽灵字段的代码**

```bash
# 在 src/ 下搜索
grep -rn "customerId\|customerName\|approvalNumber\|riskPurpose\|approvalNodes" --include="*.tsx" --include="*.ts" src/
```

对于每个匹配处：
- `customerId` → 替换为空或删除该引用
- `customerName` → 如果用于显示，改用 `companyName`
- `approvalNumber` → 删除引用（可用 `id` 替代显示）
- `riskPurpose` → 改用 `riskControlPurpose`
- `approvalNodes` → 改用 `approvalSteps`

- [ ] **Step 4: 适配 API 路由中的类型变更**

`src/app/api/risk-approvals/route.ts` 的 POST handler 中，确保字段映射只包含 Schema 定义的列（已经正确，不需要改动）。

`src/app/api/auto-approval-rules/route.ts` 的 GET handler 中，返回数据时包含新增的 `remark` 和 `isSystem` 字段：

```typescript
export async function GET() {
  const db = await getDb();
  const data = db.select().from(autoApprovalRules).all();
  return Response.json(data.map(r => ({
    ...r,
    conditions: r.conditions ? JSON.parse(r.conditions) : null,
    actions: r.actions ? JSON.parse(r.actions) : null,
  })));
}
```

（Drizzle ORM 会自动包含新增列，无需额外代码。）

- [ ] **Step 5: 验证编译**

```bash
cd project_20260526_111004/projects
npx tsc --noEmit
```

Expected: 零类型错误。如果仍有错误，逐个修复。

---

### Task 5: API 修复 — parseRecord + approval-fields PUT

**Files:**
- Modify: `src/app/api/risk-approvals/route.ts`
- Modify: `src/app/api/approval-fields/route.ts`

- [ ] **Step 1: 修复 `parseRecord` 中的 null 处理**

在 `src/app/api/risk-approvals/route.ts` 第 7-14 行：

```typescript
// 修改前
function parseRecord(record: Record<string, unknown>) {
  const parsed = { ...record };
  for (const f of JSON_FIELDS) {
    if (parsed[f]) parsed[f] = JSON.parse(parsed[f] as string);
    else parsed[f] = f === 'approvalSteps' ? [] : [];
  }
  return parsed;
}

// 修改后
function parseRecord(record: Record<string, unknown>) {
  const parsed = { ...record };
  for (const f of JSON_FIELDS) {
    if (parsed[f]) {
      parsed[f] = JSON.parse(parsed[f] as string);
    } else {
      // approvalSteps, businessCustomerIds, invoiceInfoIds → []
      // dynamicFieldValues → {}
      parsed[f] = f === 'dynamicFieldValues' ? {} : [];
    }
  }
  return parsed;
}
```

- [ ] **Step 2: 修复 `approval-fields` PUT — 部分更新保护**

在 `src/app/api/approval-fields/route.ts` 第 50-74 行的 PUT handler 中，参照 `risk-approvals` PUT 的模式，改为仅更新 `body` 中存在的字段：

```typescript
export async function PUT(req: Request) {
  try {
    const db = await getDb();
    const body = await req.json();
    if (!body.id) return Response.json({ error: 'Missing id' }, { status: 400 });
    const now = new Date().toISOString();

    const updateData: Record<string, unknown> = { updatedAt: now };

    // 字符串字段
    const STRING_FIELDS = ['name', 'fieldKey', 'fieldType', 'approvalPoint', 'status', 'remark', 'createdBy'];
    for (const field of STRING_FIELDS) {
      if (body[field] !== undefined) updateData[field] = body[field];
    }

    // JSON 字段
    if (body.serviceProducts !== undefined) updateData.serviceProducts = JSON.stringify(body.serviceProducts);
    if (body.options !== undefined) updateData.options = JSON.stringify(body.options);

    // 布尔字段
    if (body.isRequired !== undefined) updateData.isRequired = body.isRequired ? 1 : 0;

    (db.update(approvalFields) as any).set(updateData).where(eq(approvalFields.id, body.id)).run();
    return Response.json({ success: true });
  } catch (e) {
    return Response.json({ error: String(e) }, { status: 500 });
  }
}
```

- [ ] **Step 3: 验证编译**

```bash
cd project_20260526_111004/projects
npx tsc --noEmit
```

---

### Task 6: API 补齐 — approval-workflows 写操作

**Files:**
- Modify: `src/app/api/approval-workflows/route.ts`

- [ ] **Step 1: 重写审批流 API，添加 POST/PUT/DELETE**

将 `src/app/api/approval-workflows/route.ts` 从仅 GET 扩展为完整 CRUD：

```typescript
import { getDb, saveDb } from '@/db';
import { approvalWorkflows } from '@/db/schema';
import { eq } from 'drizzle-orm';

export async function GET() {
  try {
    const db = await getDb();
    const data = db.select().from(approvalWorkflows).all();
    return Response.json(data.map(w => ({
      ...w,
      approvalNodes: w.approvalNodes ? JSON.parse(w.approvalNodes) : null,
    })));
  } catch (e) {
    return Response.json({ error: String(e) }, { status: 500 });
  }
}

export async function POST(req: Request) {
  try {
    const db = await getDb();
    const body = await req.json();
    const id = body.id || `aw-${Date.now()}`;
    const now = new Date().toISOString();

    const record = {
      id,
      name: body.name || '',
      serviceProduct: body.serviceProduct ?? null,
      isTradeAgency: body.isTradeAgency ? 1 : 0,
      status: body.status ?? 'active',
      approvalNodes: body.approvalNodes ? JSON.stringify(body.approvalNodes) : null,
      remark: body.remark ?? null,
      createdBy: body.createdBy ?? null,
      createdAt: now,
      updatedAt: now,
    };
    db.insert(approvalWorkflows).values(record).run();
    saveDb();
    return Response.json({ ...record, approvalNodes: body.approvalNodes, isTradeAgency: body.isTradeAgency }, { status: 201 });
  } catch (e) {
    return Response.json({ error: String(e) }, { status: 500 });
  }
}

export async function PUT(req: Request) {
  try {
    const db = await getDb();
    const body = await req.json();
    if (!body.id) return Response.json({ error: 'Missing id' }, { status: 400 });
    const now = new Date().toISOString();

    const updateData: Record<string, unknown> = { updatedAt: now };
    const STRING_FIELDS = ['name', 'serviceProduct', 'status', 'remark', 'createdBy'];
    for (const field of STRING_FIELDS) {
      if (body[field] !== undefined) updateData[field] = body[field];
    }
    if (body.isTradeAgency !== undefined) updateData.isTradeAgency = body.isTradeAgency ? 1 : 0;
    if (body.approvalNodes !== undefined) updateData.approvalNodes = JSON.stringify(body.approvalNodes);

    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    (db.update(approvalWorkflows) as any).set(updateData).where(eq(approvalWorkflows.id, body.id)).run();
    saveDb();
    return Response.json({ success: true });
  } catch (e) {
    return Response.json({ error: String(e) }, { status: 500 });
  }
}

export async function DELETE(req: Request) {
  try {
    const db = await getDb();
    const { searchParams } = new URL(req.url);
    const id = searchParams.get('id');
    if (!id) return Response.json({ error: 'Missing id' }, { status: 400 });
    db.delete(approvalWorkflows).where(eq(approvalWorkflows.id, id)).run();
    saveDb();
    return Response.json({ success: true });
  } catch (e) {
    return Response.json({ error: String(e) }, { status: 500 });
  }
}
```

- [ ] **Step 2: 验证编译**

```bash
cd project_20260526_111004/projects
npx tsc --noEmit
```

---

### Task 7: API 补齐 — auto-approval-rules 写操作

**Files:**
- Modify: `src/app/api/auto-approval-rules/route.ts`

- [ ] **Step 1: 重写自动规则 API，添加 POST/PUT/DELETE**

将 `src/app/api/auto-approval-rules/route.ts` 从仅 GET 扩展为完整 CRUD：

```typescript
import { getDb, saveDb } from '@/db';
import { autoApprovalRules } from '@/db/schema';
import { eq } from 'drizzle-orm';

export async function GET() {
  try {
    const db = await getDb();
    const data = db.select().from(autoApprovalRules).all();
    return Response.json(data.map(r => ({
      ...r,
      conditions: r.conditions ? JSON.parse(r.conditions) : null,
      actions: r.actions ? JSON.parse(r.actions) : null,
    })));
  } catch (e) {
    return Response.json({ error: String(e) }, { status: 500 });
  }
}

export async function POST(req: Request) {
  try {
    const db = await getDb();
    const body = await req.json();
    const id = body.id || `ar-${Date.now()}`;
    const now = new Date().toISOString();

    const record = {
      id,
      name: body.name || '',
      serviceProduct: body.serviceProduct ?? null,
      status: body.status ?? 'active',
      conditionLogic: body.conditionLogic ?? 'AND',
      conditions: body.conditions ? JSON.stringify(body.conditions) : null,
      actions: body.actions ? JSON.stringify(body.actions) : null,
      successMessage: body.successMessage ?? null,
      failureMessage: body.failureMessage ?? null,
      priority: body.priority ?? 99,
      remark: body.remark ?? null,
      isSystem: body.isSystem ? 1 : 0,
      createdBy: body.createdBy ?? null,
      createdAt: now,
      updatedAt: now,
    };
    db.insert(autoApprovalRules).values(record).run();
    saveDb();
    return Response.json(
      { ...record, conditions: body.conditions, actions: body.actions, isSystem: body.isSystem },
      { status: 201 }
    );
  } catch (e) {
    return Response.json({ error: String(e) }, { status: 500 });
  }
}

export async function PUT(req: Request) {
  try {
    const db = await getDb();
    const body = await req.json();
    if (!body.id) return Response.json({ error: 'Missing id' }, { status: 400 });
    const now = new Date().toISOString();

    const updateData: Record<string, unknown> = { updatedAt: now };
    const STRING_FIELDS = ['name', 'serviceProduct', 'status', 'conditionLogic', 'successMessage', 'failureMessage', 'remark', 'createdBy'];
    for (const field of STRING_FIELDS) {
      if (body[field] !== undefined) updateData[field] = body[field];
    }
    if (body.conditions !== undefined) updateData.conditions = JSON.stringify(body.conditions);
    if (body.actions !== undefined) updateData.actions = JSON.stringify(body.actions);
    if (body.priority !== undefined) updateData.priority = body.priority;
    if (body.isSystem !== undefined) updateData.isSystem = body.isSystem ? 1 : 0;

    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    (db.update(autoApprovalRules) as any).set(updateData).where(eq(autoApprovalRules.id, body.id)).run();
    saveDb();
    return Response.json({ success: true });
  } catch (e) {
    return Response.json({ error: String(e) }, { status: 500 });
  }
}

export async function DELETE(req: Request) {
  try {
    const db = await getDb();
    const { searchParams } = new URL(req.url);
    const id = searchParams.get('id');
    if (!id) return Response.json({ error: 'Missing id' }, { status: 400 });
    db.delete(autoApprovalRules).where(eq(autoApprovalRules.id, id)).run();
    saveDb();
    return Response.json({ success: true });
  } catch (e) {
    return Response.json({ error: String(e) }, { status: 500 });
  }
}
```

- [ ] **Step 2: 验证编译**

```bash
cd project_20260526_111004/projects
npx tsc --noEmit
```

---

### Task 8: Store 同步 — 补齐 API 调用

**Files:**
- Modify: `src/lib/store.tsx`

- [ ] **Step 1: 补齐 `addRiskApproval` 的 POST 调用**

在 `src/lib/store.tsx` 第 795-797 行：

```typescript
// 修改前
const addRiskApproval = useCallback((approval: Omit<RiskApproval, 'id' | 'createdAt'>) => {
  dispatch({ type: 'ADD_RISK_APPROVAL', payload: approval });
}, []);

// 修改后
const addRiskApproval = useCallback((approval: Omit<RiskApproval, 'id' | 'createdAt'>) => {
  dispatch({ type: 'ADD_RISK_APPROVAL', payload: approval });
  // 同步到数据库
  fetch('/api/risk-approvals', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(approval),
  }).catch((e: unknown) => { console.error('[API] POST risk-approvals 失败:', e); });
}, []);
```

- [ ] **Step 2: 补齐审批流模板方法的 API 调用**

在 `src/lib/store.tsx` 第 671-681 行：

```typescript
const addApprovalWorkflow = useCallback((workflow: Omit<ApprovalWorkflow, 'id' | 'createdAt' | 'updatedAt'>) => {
  dispatch({ type: 'ADD_APPROVAL_WORKFLOW', payload: workflow });
  fetch('/api/approval-workflows', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(workflow),
  }).catch((e: unknown) => { console.error('[API] POST approval-workflows 失败:', e); });
}, []);

const updateApprovalWorkflow = useCallback((id: string, updates: Partial<ApprovalWorkflow>) => {
  dispatch({ type: 'UPDATE_APPROVAL_WORKFLOW', payload: { id, updates } });
  fetch('/api/approval-workflows', {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ id, ...updates }),
  }).catch((e: unknown) => { console.error('[API] PUT approval-workflows 失败:', e); });
}, []);

const deleteApprovalWorkflow = useCallback((id: string) => {
  dispatch({ type: 'DELETE_APPROVAL_WORKFLOW', payload: id });
  fetch(`/api/approval-workflows?id=${encodeURIComponent(id)}`, { method: 'DELETE' })
    .catch((e: unknown) => { console.error('[API] DELETE approval-workflows 失败:', e); });
}, []);
```

- [ ] **Step 3: 补齐自动审批规则方法的 API 调用**

在 `src/lib/store.tsx` 第 683-693 行：

```typescript
const addAutoApprovalRule = useCallback((rule: Omit<AutoApprovalRule, 'id' | 'createdAt' | 'updatedAt'>) => {
  dispatch({ type: 'ADD_AUTO_APPROVAL_RULE', payload: rule });
  fetch('/api/auto-approval-rules', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(rule),
  }).catch((e: unknown) => { console.error('[API] POST auto-approval-rules 失败:', e); });
}, []);

const updateAutoApprovalRule = useCallback((id: string, updates: Partial<AutoApprovalRule>) => {
  dispatch({ type: 'UPDATE_AUTO_APPROVAL_RULE', payload: { id, updates } });
  fetch('/api/auto-approval-rules', {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ id, ...updates }),
  }).catch((e: unknown) => { console.error('[API] PUT auto-approval-rules 失败:', e); });
}, []);

const deleteAutoApprovalRule = useCallback((id: string) => {
  dispatch({ type: 'DELETE_AUTO_APPROVAL_RULE', payload: id });
  fetch(`/api/auto-approval-rules?id=${encodeURIComponent(id)}`, { method: 'DELETE' })
    .catch((e: unknown) => { console.error('[API] DELETE auto-approval-rules 失败:', e); });
}, []);
```

- [ ] **Step 4: 补齐审批字段方法的 API 调用**

在 `src/lib/store.tsx` 第 815-825 行：

```typescript
const addApprovalField = useCallback((field: Omit<ApprovalField, 'id' | 'createdAt'>) => {
  dispatch({ type: 'ADD_APPROVAL_FIELD', payload: field });
  fetch('/api/approval-fields', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(field),
  }).catch((e: unknown) => { console.error('[API] POST approval-fields 失败:', e); });
}, []);

const updateApprovalField = useCallback((id: string, updates: Partial<ApprovalField>) => {
  dispatch({ type: 'UPDATE_APPROVAL_FIELD', payload: { id, updates } });
  fetch('/api/approval-fields', {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ id, ...updates }),
  }).catch((e: unknown) => { console.error('[API] PUT approval-fields 失败:', e); });
}, []);

const deleteApprovalField = useCallback((id: string) => {
  dispatch({ type: 'DELETE_APPROVAL_FIELD', payload: id });
  fetch(`/api/approval-fields?id=${encodeURIComponent(id)}`, { method: 'DELETE' })
    .catch((e: unknown) => { console.error('[API] DELETE approval-fields 失败:', e); });
}, []);
```

- [ ] **Step 5: 验证编译**

```bash
cd project_20260526_111004/projects
npx tsc --noEmit
```

---

### Task 9: ID 引用渲染修复 — utils.ts 工具函数

**Files:**
- Modify: `src/lib/utils.ts`

- [ ] **Step 1: 新增 ID→名称解析工具函数**

在 `src/lib/utils.ts` 末尾添加：

```typescript
/**
 * 根据 ID 列表解析业务主客户名称
 * @param ids - 业务主客户 ID 数组
 * @param customers - 业务主客户列表（需包含 id 和 name 字段）
 * @returns 名称数组，找不到对应名称时返回 "未知(ID)"
 */
export function resolveBusinessCustomerNames(
  ids: string[],
  customers: { id: string; name: string }[]
): string[] {
  if (!ids || ids.length === 0) return [];
  return ids.map((id) => {
    const customer = customers.find((c) => c.id === id);
    return customer ? customer.name : `未知(${id})`;
  });
}

/**
 * 根据 ID 列表解析开票信息名称
 * @param ids - 开票信息 ID 数组
 * @param invoices - 开票信息列表（需包含 id 和 title 字段）
 * @returns 名称数组，找不到对应名称时返回 "未知(ID)"
 */
export function resolveInvoiceInfoNames(
  ids: string[],
  invoices: { id: string; title: string }[]
): string[] {
  if (!ids || ids.length === 0) return [];
  return ids.map((id) => {
    const invoice = invoices.find((inv) => inv.id === id);
    return invoice ? invoice.title : `未知(${id})`;
  });
}
```

- [ ] **Step 2: 验证编译**

```bash
cd project_20260526_111004/projects
npx tsc --noEmit
```

---

### Task 10: ID 引用渲染修复 — 编辑页 lookup 数据补全

**Files:**
- Modify: `src/app/approvals/[id]/edit/page.tsx`

- [ ] **Step 1: 补全编辑页的 mockBusinessCustomers 数组**

在 `src/app/approvals/[id]/edit/page.tsx` 第 29-36 行，将 lookup 数组从 bc1-bc6 扩展为与详情页一致的 bc1-bc11：

```typescript
const mockBusinessCustomers = [
  { id: 'bc1', name: '应用材料(中国)有限公司' },
  { id: 'bc2', name: '飞雅贸易(上海)有限公司' },
  { id: 'bc3', name: '荏原机械(中国)有限公司' },
  { id: 'bc4', name: '昇先创国际贸易(上海)有限公司' },
  { id: 'bc5', name: '上海华力集成电路制造有限公司' },
  { id: 'bc6', name: '苏斯贸易(上海)有限公司' },
  { id: 'bc7', name: '武汉光库科技有限公司' },
  { id: 'bc8', name: '江苏鑫华半导体科技股份有限公司' },
  { id: 'bc9', name: '上海裘瑞经贸有限公司' },
  { id: 'bc10', name: '中芯国际集成电路制造有限公司' },
  { id: 'bc11', name: '长江存储科技有限责任公司' },
];
```

- [ ] **Step 2: 补全编辑页的 mockInvoiceInfos 数组**

在同一文件第 38-44 行：

```typescript
const mockInvoiceInfos = [
  { id: 'inv1', title: '8635 - 应用材料(中国)有限公司', taxNumber: '91310000607239088X' },
  { id: 'inv2', title: '8639 - 飞雅贸易(上海)有限公司', taxNumber: '91310000607239089Y' },
  { id: 'inv3', title: '8641 - 荏原机械(中国)有限公司', taxNumber: '91310000607239090Z' },
  { id: 'inv4', title: '8644 - 昇先创国际贸易(上海)有限公司', taxNumber: '91310000607239091A' },
  { id: 'inv5', title: '8603 - 上海华力集成电路制造有限公司', taxNumber: '91310000607239092B' },
  { id: 'inv6', title: '8612 - 武汉光库科技有限公司', taxNumber: '914201007713589677' },
  { id: 'inv7', title: '8625 - 江苏鑫华半导体科技股份有限公司', taxNumber: '91320301MA1MCPLL8F' },
  { id: 'inv8', title: '8630 - 上海裘瑞经贸有限公司', taxNumber: '91310118738523211W' },
  { id: 'inv9', title: '8648 - 中芯国际集成电路制造有限公司', taxNumber: '91310000MA1FL5N06M' },
  { id: 'inv10', title: '8655 - 长江存储科技有限责任公司', taxNumber: '91420100MA4KN4K47W' },
];
```

- [ ] **Step 3: 验证编译**

```bash
cd project_20260526_111004/projects
npx tsc --noEmit
```

---

### Task 11: ID 引用渲染修复 — 新建页 lookup 数据同步

**Files:**
- Modify: `src/app/approvals/new/page.tsx`

- [ ] **Step 1: 检查并补全新建页的 mockBusinessCustomers 和 mockInvoiceInfos**

查找新建页中是否定义了相同的 lookup 数组，如果存在且不完整（只有 bc1-bc6），与 Task 10 一样补全为 bc1-bc11 和 inv1-inv10。

- [ ] **Step 2: 验证编译**

```bash
cd project_20260526_111004/projects
npx tsc --noEmit
```

---

### Task 12: 乱码数据修正 — 各页面 mock 数据

**Files:**
- Modify: `src/lib/sample-data.ts`
- Modify: `src/app/approvals/page.tsx`
- Modify: `src/app/approvals/[id]/page.tsx`

- [ ] **Step 1: 搜索所有文件中包含 `�` 字符的行**

```bash
grep -rn "�" --include="*.tsx" --include="*.ts" src/
```

- [ ] **Step 2: 逐项修正乱码**

对每个匹配行，确认正确的公司名称/文本并替换。已知：

| 乱码 | 正确 |
|------|------|
| `��ԭ��е(�й�)���޹�˾` | `荏原机械(中国)有限公司` |

如果搜索结果中未找到乱码记录（即页面 mock 数据已经是正确的），说明乱码仅在数据库文件中（`.db` 文件），需要重新执行 seed 脚本或手动修正数据库。

- [ ] **Step 3: 验证编译**

```bash
cd project_20260526_111004/projects
npx tsc --noEmit
```

---

### Task 13: 最终编译验证

- [ ] **Step 1: 完整 TypeScript 编译检查**

```bash
cd project_20260526_111004/projects
npx tsc --noEmit
```

Expected: 零类型错误。

- [ ] **Step 2: 逐页 visual check（手动）**

| 页面 | 验证项 |
|------|--------|
| `/approvals` | 荏原机械显示正常、列表无 bcX/invX |
| `/approvals/new` | 表单正常、业务主客户下拉完整 |
| `/approvals/9` | 中芯国际详情页 → 业务主客户显示名称 |
| `/approvals/9/edit` | 编辑页 → 业务主客户/开票信息正确回填 |
| `/approval/workflows` | 增删改查正常、刷新后数据保留 |
| `/approval/auto-rules` | 增删改查正常、刷新后数据保留 |
| `/approval/fields` | 增删改查正常、刷新后数据保留 |

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "fix: 风控审批模块数据修复 — 编码/字段对齐/API同步/ID渲染
- 数据库添加 UTF-8 PRAGMA 防止中文乱码
- 清理 RiskApproval/ApprovalWorkflow 类型幽灵字段
- 统一 isTradeAgent → isTradeAgency 拼写
- 补齐 approval-workflows 和 auto-approval-rules 的 POST/PUT/DELETE API
- 补齐 Store 中 10 个 mutation 方法的 API 同步调用
- 修复 parseRecord 中 dynamicFieldValues null → {} 的错误
- 修复 approval-fields PUT 的部分更新保护
- 补全编辑页/新建页的 mockBusinessCustomers 和 mockInvoiceInfos
- 添加 resolveBusinessCustomerNames / resolveInvoiceInfoNames 工具函数"
```
