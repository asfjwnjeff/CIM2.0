# 风控审批板块升级 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将风控审批模块从硬编码升级为全可配置化：审批字段可配置、审批流模板可配置、自动规则产出审批辅助报告、规则可动态追加审批人/会签。

**Architecture:** 新增审批字段配置模块（`/approval/fields`），改造审批流模板（`/approval/workflows`），保留自动审批规则不动，重构审批申请页（`/approvals/new`、`/[id]`、`/[id]/edit`）实现动态字段+审批流实时联动+进度可视化，新增审批辅助报告组件。

**Tech Stack:** Next.js 16.1 + React 19 + TypeScript 5 + shadcn/ui + Tailwind CSS v4 + Drizzle ORM (SQLite) + React Context + useReducer

**Spec Reference:** `docs/superpowers/specs/2026-05-29-风控审批升级设计.md`

---

## 文件结构总览

### 新增文件
| 文件 | 用途 |
|------|------|
| `src/app/approval/fields/page.tsx` | 审批字段配置列表页 |
| `src/app/api/approval-fields/route.ts` | 审批字段 CRUD API |
| `src/components/ApprovalReport.tsx` | 审批辅助报告组件 |
| `src/components/ApprovalFlowVisual.tsx` | 审批流可视化组件（从现有页面提取） |

### 修改文件
| 文件 | 改动 |
|------|------|
| `src/lib/types.ts` | 新增 `ApprovalField` 类型 |
| `src/lib/store.tsx` | 新增 fields state + actions，添加规则引擎函数 |
| `src/lib/sample-data.ts` | 新增初始审批字段数据 |
| `src/lib/navigation.tsx` | 添加审批字段配置菜单 |
| `src/db/schema.ts` | 新增 `approval_fields` 表 |
| `src/app/approval/workflows/new/page.tsx` | 审批流模板编辑页重构 |
| `src/app/approval/workflows/page.tsx` | 审批流模板列表页优化 |
| `src/app/approval/workflows/[id]/page.tsx` | 审批流模板详情页优化 |
| `src/app/approvals/new/page.tsx` | 重构为动态字段+审批流联动 |
| `src/app/approvals/[id]/page.tsx` | 审批详情增加报告 Tab + 进度可视化 |
| `src/app/approvals/[id]/edit/page.tsx` | 重构为动态字段+审批流联动 |
| `src/app/approvals/page.tsx` | 列表页适配 |
| `src/app/globals.css` | 新增审批流状态动画 keyframes |

### 不修改文件
| 文件 | 原因 |
|------|------|
| `src/app/approval/auto-rules/**` | 保留不动，后续测试后再评估 |

---

## Phase 1: 数据层

### Task 1.1: 新增 ApprovalField 类型定义

**Files:**
- Modify: `project_20260526_111004/projects/src/lib/types.ts`

- [ ] **Step 1: 在 types.ts 中新增类型**

在 `ApprovalNodeType` 附近（约 536 行）插入：

```typescript
// ========== 审批字段配置 ==========

/** 审批字段类型 */
export type ApprovalFieldType =
  | 'number_select'    // 数值（下拉）
  | 'percentage'       // 百分比
  | 'single_select'    // 单选
  | 'multi_select'     // 多选
  | 'single_other'     // 单选+其他
  | 'boolean'          // 布尔
  | 'api_query'        // 接口查询
  | 'text';            // 文本输入

/** 审批字段选项 */
export interface ApprovalFieldOption {
  id: string;
  label: string;       // 选项文本（标签即值，不做分离）
  order: number;
}

/** 审批结构化字段 */
export interface ApprovalField {
  id: string;
  name: string;                      // 字段名称（中文显示）
  fieldKey: string;                  // 字段Key（英文标识）
  fieldType: ApprovalFieldType;      // 字段类型
  serviceProducts: ServiceProduct[]; // 绑定的服务产品
  options: ApprovalFieldOption[];    // 可选值列表
  isRequired: boolean;               // 是否必填
  approvalPoint?: string;            // 关联审批点名称（如"业务量"）
  status: Status;                    // active | inactive
  remark?: string;
  createdBy?: string;
  createdAt: string;
  updatedAt?: string;
}
```

- [ ] **Step 2: 类型检查**

```bash
cd project_20260526_111004/projects && npx tsc --noEmit --pretty 2>&1 | head -20
```

Expected: 无新增类型错误。

- [ ] **Step 3: 提交**

```bash
git add project_20260526_111004/projects/src/lib/types.ts
git commit -m "feat: 新增 ApprovalField 审批字段配置类型定义"
```

---

### Task 1.2: 新增 approval_fields 数据库表

**Files:**
- Modify: `project_20260526_111004/projects/src/db/schema.ts`

- [ ] **Step 1: 在 schema.ts 中新增表定义**

在 `autoApprovalRules` 表定义之后追加（约 115 行）：

```typescript
// 审批字段配置表
export const approvalFields = sqliteTable('approval_fields', {
  id: text('id').primaryKey(),
  name: text('name').notNull(),
  fieldKey: text('field_key').notNull(),
  fieldType: text('field_type').notNull().default('text'),
  serviceProducts: text('service_products').notNull().default('[]'), // JSON array
  options: text('options').notNull().default('[]'),                   // JSON array
  isRequired: integer('is_required').notNull().default(0),            // boolean
  approvalPoint: text('approval_point'),
  status: text('status').notNull().default('active'),
  remark: text('remark'),
  createdBy: text('created_by'),
  createdAt: text('created_at').notNull().default(sql`(datetime('now'))`),
  updatedAt: text('updated_at'),
});
```

- [ ] **Step 2: 推送 schema 到数据库**

```bash
cd project_20260526_111004/projects && npx drizzle-kit push
```

Expected: `approval_fields` 表创建成功。

- [ ] **Step 3: 提交**

```bash
git add project_20260526_111004/projects/src/db/schema.ts
git commit -m "feat: 新增 approval_fields 数据库表定义"
```

---

### Task 1.3: 新增审批字段 API 路由

**Files:**
- Create: `project_20260526_111004/projects/src/app/api/approval-fields/route.ts`

- [ ] **Step 1: 创建 API 路由文件**

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { getDb } from '@/db';
import { approvalFields } from '@/db/schema';
import { eq } from 'drizzle-orm';
import { v4 as uuidv4 } from 'uuid';

/** GET /api/approval-fields — 获取全部字段 */
export async function GET() {
  try {
    const db = await getDb();
    const data = db.select().from(approvalFields).all();
    const parsed = data.map((f: Record<string, unknown>) => ({
      ...f,
      serviceProducts: f.serviceProducts ? JSON.parse(f.serviceProducts as string) : [],
      options: f.options ? JSON.parse(f.options as string) : [],
      isRequired: f.isRequired === 1,
    }));
    return NextResponse.json(parsed);
  } catch (e) {
    return NextResponse.json({ error: String(e) }, { status: 500 });
  }
}

/** POST /api/approval-fields — 新增字段 */
export async function POST(req: NextRequest) {
  try {
    const db = await getDb();
    const body = await req.json();
    const id = uuidv4();
    const now = new Date().toISOString();

    const record = {
      id,
      name: body.name,
      fieldKey: body.fieldKey,
      fieldType: body.fieldType ?? 'text',
      serviceProducts: JSON.stringify(body.serviceProducts ?? []),
      options: JSON.stringify(body.options ?? []),
      isRequired: body.isRequired ? 1 : 0,
      approvalPoint: body.approvalPoint ?? null,
      status: body.status ?? 'active',
      remark: body.remark ?? null,
      createdBy: body.createdBy ?? null,
      createdAt: now,
      updatedAt: now,
    };
    db.insert(approvalFields).values(record).run();
    return NextResponse.json({ ...record, serviceProducts: body.serviceProducts, options: body.options, isRequired: body.isRequired }, { status: 201 });
  } catch (e) {
    return NextResponse.json({ error: String(e) }, { status: 500 });
  }
}

/** PUT /api/approval-fields — 更新字段 */
export async function PUT(req: NextRequest) {
  try {
    const db = await getDb();
    const body = await req.json();
    const now = new Date().toISOString();

    db.update(approvalFields)
      .set({
        name: body.name,
        fieldKey: body.fieldKey,
        fieldType: body.fieldType,
        serviceProducts: JSON.stringify(body.serviceProducts),
        options: JSON.stringify(body.options),
        isRequired: body.isRequired ? 1 : 0,
        approvalPoint: body.approvalPoint ?? null,
        status: body.status,
        remark: body.remark ?? null,
        updatedAt: now,
      })
      .where(eq(approvalFields.id, body.id))
      .run();
    return NextResponse.json({ success: true });
  } catch (e) {
    return NextResponse.json({ error: String(e) }, { status: 500 });
  }
}

/** DELETE /api/approval-fields — 删除字段 */
export async function DELETE(req: NextRequest) {
  try {
    const db = await getDb();
    const { searchParams } = new URL(req.url);
    const id = searchParams.get('id');
    if (!id) return NextResponse.json({ error: 'Missing id' }, { status: 400 });
    db.delete(approvalFields).where(eq(approvalFields.id, id)).run();
    return NextResponse.json({ success: true });
  } catch (e) {
    return NextResponse.json({ error: String(e) }, { status: 500 });
  }
}
```

- [ ] **Step 2: 验证 API**

```bash
cd project_20260526_111004/projects && curl -s http://localhost:3000/api/approval-fields | head -50
```

Expected: 返回空数组 `[]` 或已有种子数据。

- [ ] **Step 3: 提交**

```bash
git add project_20260526_111004/projects/src/app/api/approval-fields/route.ts
git commit -m "feat: 新增审批字段 CRUD API 路由"
```

---

### Task 1.4: 新增审批字段种子数据

**Files:**
- Modify: `project_20260526_111004/projects/src/lib/sample-data.ts`

- [ ] **Step 1: 在 sample-data.ts 末尾新增种子数据**

```typescript
/** 初始审批结构化字段 */
export const initialApprovalFields: ApprovalField[] = [
  // ===== 运输 =====
  {
    id: 'af-001', name: '月订单数', fieldKey: 'monthly_orders', fieldType: 'number_select',
    serviceProducts: ['运输'], approvalPoint: '业务量',
    options: [
      { id: 'opt-001', label: '0-5单', order: 1 },
      { id: 'opt-002', label: '6-10单', order: 2 },
      { id: 'opt-003', label: '11-20单', order: 3 },
      { id: 'opt-004', label: '21-50单', order: 4 },
      { id: 'opt-005', label: '50单以上', order: 5 },
    ],
    isRequired: true, status: 'active',
    remark: '评估运输业务订单量是否达到基本业务规模门槛',
    createdBy: 'system', createdAt: '2026-05-29T00:00:00Z',
  },
  {
    id: 'af-002', name: '月开票额', fieldKey: 'monthly_invoice_amount', fieldType: 'number_select',
    serviceProducts: ['运输'], approvalPoint: '业务量',
    options: [
      { id: 'opt-006', label: '0-5000元', order: 1 },
      { id: 'opt-007', label: '5000-20000元', order: 2 },
      { id: 'opt-008', label: '20000-100000元', order: 3 },
      { id: 'opt-009', label: '100000元以上', order: 4 },
    ],
    isRequired: true, status: 'active',
    createdBy: 'system', createdAt: '2026-05-29T00:00:00Z',
  },
  {
    id: 'af-003', name: '运输及时率', fieldKey: 'on_time_rate', fieldType: 'percentage',
    serviceProducts: ['运输'], approvalPoint: 'KPI时效考核要求',
    options: [],
    isRequired: false, status: 'active',
    createdBy: 'system', createdAt: '2026-05-29T00:00:00Z',
  },
  {
    id: 'af-004', name: '意向服务地区', fieldKey: 'service_regions', fieldType: 'multi_select',
    serviceProducts: ['运输'], approvalPoint: '业务分布',
    options: [
      { id: 'opt-010', label: '上海', order: 1 },  { id: 'opt-011', label: '无锡', order: 2 },
      { id: 'opt-012', label: '南京', order: 3 },  { id: 'opt-013', label: '合肥', order: 4 },
      { id: 'opt-014', label: '杭州', order: 5 },  { id: 'opt-015', label: '武汉', order: 6 },
      { id: 'opt-016', label: '成都', order: 7 },  { id: 'opt-017', label: '西安', order: 8 },
      { id: 'opt-018', label: '北京', order: 9 },  { id: 'opt-019', label: '大连', order: 10 },
      { id: 'opt-020', label: '厦门', order: 11 }, { id: 'opt-021', label: '深圳', order: 12 },
      { id: 'opt-022', label: '广州', order: 13 },
    ],
    isRequired: true, status: 'active',
    createdBy: 'system', createdAt: '2026-05-29T00:00:00Z',
  },

  // ===== 货代 =====
  {
    id: 'af-005', name: '运输产品种类', fieldKey: 'transport_product_type', fieldType: 'single_other',
    serviceProducts: ['货代'], approvalPoint: '运输产品种类',
    options: [
      { id: 'opt-023', label: '空运进口', order: 1 },
      { id: 'opt-024', label: '海运进口', order: 2 },
      { id: 'opt-025', label: '公路进口', order: 3 },
      { id: 'opt-026', label: '海运一贯式运输', order: 4 },
      { id: 'opt-027', label: '空运出口', order: 5 },
      { id: 'opt-028', label: '海运出口', order: 6 },
      { id: 'opt-029', label: '公路出口', order: 7 },
      { id: 'opt-030', label: '其他', order: 8 },
    ],
    isRequired: true, status: 'active',
    createdBy: 'system', createdAt: '2026-05-29T00:00:00Z',
  },
  {
    id: 'af-006', name: '出货国家/地区', fieldKey: 'shipping_country', fieldType: 'single_select',
    serviceProducts: ['货代'], approvalPoint: '出货地区',
    options: [
      { id: 'opt-031', label: '中国', order: 1 },
      { id: 'opt-032', label: '美国', order: 2 },
      { id: 'opt-033', label: '越南', order: 3 },
      { id: 'opt-034', label: '以色列', order: 4 },
      { id: 'opt-035', label: '伊朗', order: 5 },
    ],
    isRequired: true, status: 'active',
    createdBy: 'system', createdAt: '2026-05-29T00:00:00Z',
  },
  {
    id: 'af-007', name: '注册资本', fieldKey: 'registered_capital', fieldType: 'text',
    serviceProducts: ['货代', '仓库', '运输'], approvalPoint: '企业规模',
    options: [],
    isRequired: true, status: 'active',
    remark: '可通过企查查接口自动获取',
    createdBy: 'system', createdAt: '2026-05-29T00:00:00Z',
  },
  {
    id: 'af-008', name: '社保人数', fieldKey: 'social_insurance_count', fieldType: 'text',
    serviceProducts: ['货代', '仓库', '运输'], approvalPoint: '企业规模',
    options: [],
    isRequired: true, status: 'active',
    remark: '可通过企查查接口自动获取',
    createdBy: 'system', createdAt: '2026-05-29T00:00:00Z',
  },
  {
    id: 'af-009', name: '是否贸易代理', fieldKey: 'is_trade_agent', fieldType: 'boolean',
    serviceProducts: ['运输', '货代', '仓库', '关务', '进出口', '维修', '合同物流'],
    approvalPoint: '业务类型',
    options: [],
    isRequired: true, status: 'active',
    createdBy: 'system', createdAt: '2026-05-29T00:00:00Z',
  },

  // ===== 仓库 =====
  {
    id: 'af-010', name: '面积需求', fieldKey: 'area_requirement', fieldType: 'number_select',
    serviceProducts: ['仓库'], approvalPoint: '业务量',
    options: [
      { id: 'opt-036', label: '0-100㎡', order: 1 },
      { id: 'opt-037', label: '100-500㎡', order: 2 },
      { id: 'opt-038', label: '500-1000㎡', order: 3 },
      { id: 'opt-039', label: '1000㎡以上', order: 4 },
    ],
    isRequired: true, status: 'active',
    createdBy: 'system', createdAt: '2026-05-29T00:00:00Z',
  },
  {
    id: 'af-011', name: '货量需求', fieldKey: 'cargo_volume', fieldType: 'number_select',
    serviceProducts: ['仓库'], approvalPoint: '业务量',
    options: [
      { id: 'opt-040', label: '0-100吨/月', order: 1 },
      { id: 'opt-041', label: '100-500吨/月', order: 2 },
      { id: 'opt-042', label: '500吨以上/月', order: 3 },
    ],
    isRequired: true, status: 'active',
    createdBy: 'system', createdAt: '2026-05-29T00:00:00Z',
  },
  {
    id: 'af-012', name: '地域需求', fieldKey: 'warehouse_region', fieldType: 'single_select',
    serviceProducts: ['仓库'], approvalPoint: '业务量',
    options: [
      { id: 'opt-043', label: '上海', order: 1 },
      { id: 'opt-044', label: '武汉', order: 2 },
      { id: 'opt-045', label: '成都', order: 3 },
      { id: 'opt-046', label: '其他', order: 4 },
    ],
    isRequired: true, status: 'active',
    createdBy: 'system', createdAt: '2026-05-29T00:00:00Z',
  },
  {
    id: 'af-013', name: '存储类型', fieldKey: 'storage_type', fieldType: 'single_select',
    serviceProducts: ['仓库'], approvalPoint: '存储类型',
    options: [
      { id: 'opt-047', label: '恒温恒湿', order: 1 },
      { id: 'opt-048', label: '常温', order: 2 },
    ],
    isRequired: true, status: 'active',
    createdBy: 'system', createdAt: '2026-05-29T00:00:00Z',
  },
];
```

- [ ] **Step 2: 在 store.tsx 中引入种子数据**

在 store.tsx 文件头部 import 区添加：
```typescript
import { initialApprovalFields } from './sample-data';
```

在 state 初始化中添加：
```typescript
approvalFields: initialApprovalFields,
```

- [ ] **Step 3: 类型检查**

```bash
cd project_20260526_111004/projects && npx tsc --noEmit --pretty 2>&1 | head -20
```

- [ ] **Step 4: 提交**

```bash
git add project_20260526_111004/projects/src/lib/sample-data.ts project_20260526_111004/projects/src/lib/store.tsx
git commit -m "feat: 新增审批字段种子数据并接入 store"
```

---

### Task 1.5: 更新 Store — 审批字段 state + actions

**Files:**
- Modify: `project_20260526_111004/projects/src/lib/store.tsx`

- [ ] **Step 1: 新增 type/interface**

在 State 接口中添加（约 55 行附近）：
```typescript
approvalFields: ApprovalField[];
```

在 Actions 接口中添加（约 112 行附近）：
```typescript
addApprovalField: (field: ApprovalField) => void;
updateApprovalField: (field: ApprovalField) => void;
deleteApprovalField: (id: string) => void;
```

- [ ] **Step 2: 新增 action types**

```typescript
ADD_APPROVAL_FIELD = 'ADD_APPROVAL_FIELD',
UPDATE_APPROVAL_FIELD = 'UPDATE_APPROVAL_FIELD',
DELETE_APPROVAL_FIELD = 'DELETE_APPROVAL_FIELD',
```

- [ ] **Step 3: 新增 reducer case**

```typescript
case 'ADD_APPROVAL_FIELD':
  return { ...state, approvalFields: [...state.approvalFields, action.payload] };
case 'UPDATE_APPROVAL_FIELD':
  return {
    ...state,
    approvalFields: state.approvalFields.map(f =>
      f.id === action.payload.id ? action.payload : f
    ),
  };
case 'DELETE_APPROVAL_FIELD':
  return {
    ...state,
    approvalFields: state.approvalFields.filter(f => f.id !== action.payload),
  };
```

- [ ] **Step 4: 新增 useCallback 实现**

参考现有 `addApprovalWorkflow` 模式，添加三个 useCallback 函数。

- [ ] **Step 5: 提交**

```bash
git add project_20260526_111004/projects/src/lib/store.tsx
git commit -m "feat: store 新增审批字段 CRUD state/actions"
```

---

## Phase 2: 审批字段配置页面

### Task 2.1: 创建审批字段配置列表页

**Files:**
- Create: `project_20260526_111004/projects/src/app/approval/fields/page.tsx`

- [ ] **Step 1: 创建页面**

页面结构：
- `'use client'` 声明
- 使用 `useApp` hook 获取 `approvalFields`、`addApprovalField`、`updateApprovalField`、`deleteApprovalField`、`deleteApprovalField`
- 表格展示：字段名称 / Key / 类型 / 绑定服务产品 / 审批点 / 状态 / 操作
- 操作栏：搜索框 + 服务产品筛选 + 「新增字段」按钮
- 新增/编辑弹窗：Dialog 表单组件
- 表单字段：名称、Key、类型（下拉选择8种类型）、绑定服务产品（多选）、审批点、可选值配置区（根据类型显示/隐藏）、是否必填、状态、备注

**关键实现要点：**
1. 字段类型为 `number_select`、`single_select`、`multi_select`、`single_other` 时显示可选值配置区
2. 可选值配置区支持动态增删
3. 绑定服务产品使用 SearchableMultiSelect 组件
4. 操作列：编辑 / 停用（状态切换）
5. 弹窗表单使用 `FormEvent` 类型处理提交

完整代码需要包含所有状态管理（dialogOpen、editingField、searchQuery、filterProduct 等）、表单验证、以及提交逻辑。

- [ ] **Step 2: 验证页面**

启动开发服务器，访问 `http://localhost:3000/approval/fields`，确认：
- 表格正确展示种子数据中的 13 个字段
- 搜索和筛选功能正常
- 新增/编辑弹窗可正常打开和提交

- [ ] **Step 3: 提交**

```bash
git add project_20260526_111004/projects/src/app/approval/fields/page.tsx
git commit -m "feat: 新增审批字段配置列表页"
```

---

## Phase 3: 审批流模板改造

### Task 3.1: 审批流模板列表页优化

**Files:**
- Modify: `project_20260526_111004/projects/src/app/approval/workflows/page.tsx`

- [ ] **Step 1: 页面改进**

现列表页已是完整功能。本次优化：
1. 统一使用 store 数据源（`approvalWorkflows`），不再混用页面内部 mock
2. 列表展示字段改为：模板名称 / 服务产品 / 节点数 / 状态 / 更新时间 / 操作
3. 操作列增加「复制」按钮，点击后复制一份模板并打开编辑（服务产品可为空，需用户选择）
4. 服务产品列显示为 Badge 样式

- [ ] **Step 2: 提交**

```bash
git add project_20260526_111004/projects/src/app/approval/workflows/page.tsx
git commit -m "refactor: 审批流模板列表页优化 — 统一 store 数据源 + 复制功能"
```

---

### Task 3.2: 审批流模板编辑页重构

**Files:**
- Modify: `project_20260526_111004/projects/src/app/approval/workflows/new/page.tsx`

- [ ] **Step 1: 重构编辑页面**

重新设计节点配置区域，每个节点卡片包含：
1. **固定节点（1/2/4/5/6）**：灰色圆形编号 + 节点名称 + 搜索添加/移除审批人
2. **动态节点（3-职能审批人）**：蓝色圆形编号 + 蓝色卡片底色 + 「动态」标签 + 默认审批人展示 + 条件分支审批人占位区域

关键交互：
- 每个审批人可搜索添加，已添加的审批人显示为 Tag 附 × 移除按钮
- 职能审批节点底部预留条件分支审批人区域（虚线边框 + 浅黄底色），标注"由自动审批规则动态追加"
- 节点间用竖线连接

- [ ] **Step 2: 更新审批节点数据模型**

在保存审批流模板时，`approvalNodes` 中职能审批节点增加 `conditionalApprovers` 字段：

```typescript
// ApprovalNode 类型扩展
interface ApprovalNode {
  // ... 现有字段
  conditionalApprovers?: ApprovalNodeApprover[]; // 预留的条件分支审批人
}
```

- [ ] **Step 3: 提交**

```bash
git add project_20260526_111004/projects/src/app/approval/workflows/new/page.tsx project_20260526_111004/projects/src/lib/types.ts
git commit -m "refactor: 审批流模板编辑页 — 动态节点可视化 + 条件分支预留"
```

---

### Task 3.3: 审批流模板详情页优化

**Files:**
- Modify: `project_20260526_111004/projects/src/app/approval/workflows/[id]/page.tsx`

- [ ] **Step 1: 更新详情页**

1. 审批流程 Tab 中的节点展示与编辑页保持一致的视觉风格
2. 动态节点同样以蓝色高亮显示
3. 自动审批规则 Tab 保留不变

- [ ] **Step 2: 提交**

```bash
git add project_20260526_111004/projects/src/app/approval/workflows/[id]/page.tsx
git commit -m "refactor: 审批流模板详情页 — 节点视觉与编辑页统一"
```

---

## Phase 4: 审批申请页重构

### Task 4.1: 提取审批流可视化组件

**Files:**
- Create: `project_20260526_111004/projects/src/components/ApprovalFlowVisual.tsx`

- [ ] **Step 1: 创建审批流可视化组件**

从现有审批申请新建页中提取审批流展示逻辑为独立组件。

**Props 接口：**
```typescript
interface ApprovalFlowVisualProps {
  workflow: ApprovalWorkflow | null;               // 匹配的审批流模板
  ruleTriggeredApprovers: RuleTriggeredApprover[]; // 规则触发的追加审批人
  mode: 'preview' | 'progress';                    // 预览模式 or 进度模式
  progress?: {                                      // 进度模式下的状态
    currentNodeIndex: number;
    completedNodes: number[];
    approvalTimes?: Record<number, string>;
  };
}

interface RuleTriggeredApprover {
  approver: ApprovalNodeApprover;
  reason: string;  // 触发原因，如"涉及贸易代理"
  ruleId: string;
}
```

**组件行为：**
- `mode='preview'`（新建/编辑页使用）：
  - 固定节点灰色圆形编号
  - 职能审批节点蓝色高亮 + 蓝色卡片
  - 展示默认审批人 + 条件分支追加审批人（原因标注）
  - 追加审批人去重展示
  - 多人时底部显示会签提示

- `mode='progress'`（详情页使用）：
  - 已完成节点：绿色 ✓ + 绿色连线 + 完成时间
  - 当前节点：蓝色光晕 + 蓝色卡片 + 「审批中」标签
  - 待处理节点：灰色描边 + 灰色文字
  - 动态节点展示完整匹配原因

- [ ] **Step 2: 在 globals.css 中添加动画**

```css
@keyframes approval-pulse {
  0%, 100% { box-shadow: 0 0 0 3px rgba(45, 59, 255, 0.15); }
  50% { box-shadow: 0 0 0 6px rgba(45, 59, 255, 0.08); }
}
```

- [ ] **Step 3: 提交**

```bash
git add project_20260526_111004/projects/src/components/ApprovalFlowVisual.tsx project_20260526_111004/projects/src/app/globals.css
git commit -m "feat: 提取审批流可视化组件 — 支持预览/进度两种模式"
```

---

### Task 4.2: 重构新建审批申请页

**Files:**
- Modify: `project_20260526_111004/projects/src/app/approvals/new/page.tsx`

- [ ] **Step 1: 重构双栏布局**

保持现有双栏布局，在此基础上新增：

**左侧表单区域：**

1. **4组字段分区**，每组有色标 + 标题：
   - 基础信息（蓝色色标）：公司全称、英文名称、商机、服务产品、业务类型、风险控制目的
   - 业务评估（橙色色标）：货物类型、月均业务量、每月开票金额、通关/运输/仓库KPI要求
   - 合规审核（绿色色标）：动态结构化字段区域
   - 结算与合同（灰色色标）：HMG关系、业务主客户、建议系统代码、客户开票信息、结算账期、联系人、定制化服务需求/描述、集团/分子公司名称

2. **动态字段加载逻辑**：
```typescript
// 根据选中的服务产品过滤动态字段
const dynamicFields = approvalFields.filter(
  f => f.status === 'active' && f.serviceProducts.includes(selectedServiceProduct)
);
```

3. **动态字段渲染**：
   - 使用浅蓝底色输入框（`bg-[#F0F1FF] border-[#C7CAFF]`）
   - "是否贸易代理=是"时该字段变为浅黄底色（`bg-[#FFF9EB] border-[#E8850C]`）
   - 根据 `fieldType` 渲染不同的输入组件（Select/Input/Textarea/Toggle）

4. **实时规则检查侧边栏**（右侧审批流预览下方）：
   - 显示已填写字段的通过/提醒状态
   - 统计通过/提醒/待检查项数量

**右侧审批流区域：**
- 使用 `ApprovalFlowVisual` 组件，mode='preview'
- 根据选中的服务产品自动匹配审批流模板
- 根据已填写的动态字段值，模拟规则触发效果

**底部操作栏：**
- 「清空」「暂存」「预览报告」「提交审批」四个按钮
- 「提交审批」为主要操作，蓝色按钮
- 「预览报告」点击后弹窗展示审批辅助报告预览

- [ ] **Step 2: 验证交互**

1. 切换服务产品 → 动态字段区自动切换 → 审批流模板自动匹配
2. 填写"是否贸易代理=是" → 字段底色变黄 → 审批流追加白沥
3. 填写其他触发条件 → 对应的审批人动态追加
4. 多字段触发 → 审批人去重 → 会签提示

- [ ] **Step 3: 提交**

```bash
git add project_20260526_111004/projects/src/app/approvals/new/page.tsx
git commit -m "feat: 重构新建审批申请页 — 动态字段 + 审批流联动 + 实时规则检查"
```

---

### Task 4.3: 重构编辑审批申请页

**Files:**
- Modify: `project_20260526_111004/projects/src/app/approvals/[id]/edit/page.tsx`

- [ ] **Step 1: 与新建页保持一致的交互**

编辑页与新建页共享相同的表单结构和审批流可视化逻辑。区别在于：
- 初始数据从已有审批记录中加载
- 服务产品切换时需确认是否清空已填的动态字段

- [ ] **Step 2: 提交**

```bash
git add project_20260526_111004/projects/src/app/approvals/[id]/edit/page.tsx
git commit -m "refactor: 重构编辑审批申请页 — 与新建页一致的动态字段交互"
```

---

### Task 4.4: 重构审批详情页（进度可视化 + 报告 Tab）

**Files:**
- Modify: `project_20260526_111004/projects/src/app/approvals/[id]/page.tsx`

- [ ] **Step 1: 重构详情页**

1. **审批进度可视化**：使用 `ApprovalFlowVisual` 组件，mode='progress'，展示审批流当前进度（已完成/当前/待处理）

2. **新增「审批辅助报告」Tab**：
   - 与现有 Tab 并列
   - 展示审批辅助报告组件（如该审批已提交）
   - 如果审批未提交（草稿状态），显示"审批尚未提交，暂无报告"

3. **保留现有字段只读展示**

- [ ] **Step 2: 提交**

```bash
git add project_20260526_111004/projects/src/app/approvals/[id]/page.tsx
git commit -m "feat: 重构审批详情页 — 进度可视化 + 审批辅助报告 Tab"
```

---

## Phase 5: 审批辅助报告

### Task 5.1: 创建审批辅助报告组件

**Files:**
- Create: `project_20260526_111004/projects/src/components/ApprovalReport.tsx`

- [ ] **Step 1: 创建报告组件**

**Props 接口：**
```typescript
interface ReportItem {
  approvalPoint: string;    // 审批点
  fieldName: string;        // 字段名
  fieldKey: string;         // 字段 Key
  fieldValue: string;       // 填写值
  condition: string;        // 判断条件
  result: 'pass' | 'warn' | 'reject';  // 结果
  suggestion?: string;      // 建议文案（warn/reject 时）
}

interface ApprovalReportProps {
  reportId: string;
  customerName: string;
  serviceProduct: string;
  generatedAt: string;
  items: ReportItem[];
  flowChanges: string;     // 审批流变更摘要，如"未触发调整"
  passCount: number;
  warnCount: number;
  rejectCount: number;
}
```

**组件布局（紧凑表格）：**
```
┌─────────────────────────────────────────────────┐
│ 报告编号 RA-xxx · 生成时间 · 客户 · 服务产品     │
│                    3通过  2提醒  0拦截           │
├─────────────────────────────────────────────────┤
│ 审批点 │ 字段 │ 填写值 │ 判断条件                │
├─────────────────────────────────────────────────┤
│ ▌● KPI时效考核 │ 运输及时率 │ 95% │ 及时率<99%  │  (绿色)
│ ▌● 出货地区 │ 出货国家 │ 中国 │ 非战争地区       │  (绿色)
│ ▌● 业务类型 │ 是否贸易代理 │ 否 │ 非贸易代理     │  (绿色)
│ ▌● 业务量 │ 月订单数 │ 0-5单 │ ≤5单★★           │  (黄色)
│ ▌● 企业规模 │ 注册资本 │ 80万 │ <100万 ⚠人核    │  (黄色)
├─────────────────────────────────────────────────┤
│ 审批流变更：未触发调整     CIM2.0 自动审批引擎   │
└─────────────────────────────────────────────────┘
```

**关键样式：**
- 通过行：左侧 `border-l-[3px] border-l-[#0D8A5E]` + 行内绿色圆点
- 提醒行：左侧 `border-l-[3px] border-l-[#E8850C]` + 行内黄色圆点 + 浅黄底色 `bg-[#FFF9EB]`
- 拦截行：左侧 `border-l-[3px] border-l-[#D63031]` + 行内红色圆点
- 顶部摘要：紧凑一行展示

- [ ] **Step 2: 提交**

```bash
git add project_20260526_111004/projects/src/components/ApprovalReport.tsx
git commit -m "feat: 新增审批辅助报告组件"
```

---

### Task 5.2: 规则引擎函数

**Files:**
- Modify: `project_20260526_111004/projects/src/lib/store.tsx`

- [ ] **Step 1: 新增规则评估 + 报告生成函数**

在 store.tsx 中新增两个导出函数：

```typescript
/** 评估自动审批规则，返回每个审批点的结果 */
export function evaluateApprovalRules(
  rules: AutoApprovalRule[],
  fieldValues: Record<string, string>,
): Map<string, { result: 'pass' | 'warn' | 'reject'; suggestion?: string; triggeredRules: string[] }>

/** 根据规则评估结果，计算需要追加的审批人（去重） */
export function computeTriggeredApprovers(
  rules: AutoApprovalRule[],
  fieldValues: Record<string, string>,
  serviceProduct: ServiceProduct,
): RuleTriggeredApprover[]
```

评估逻辑：
1. 遍历所有 active 规则，检查 conditions
2. 条件匹配 → 根据 actionType 产出：
   - `auto_approve` → `result: 'pass'`
   - `show_message` → `result: 'warn'` + suggestion
   - `auto_reject` → `result: 'reject'`
   - `add_approver` → 收集到 triggeredApprovers
3. 同一审批人被多个规则触发 → 去重（合并 reason）
4. 返回结果 Map + 追加审批人列表

- [ ] **Step 2: 提交**

```bash
git add project_20260526_111004/projects/src/lib/store.tsx
git commit -m "feat: store 新增规则引擎 evaluateApprovalRules + computeTriggeredApprovers"
```

---

### Task 5.3: 在审批申请页集成报告生成

**Files:**
- Modify: `project_20260526_111004/projects/src/app/approvals/new/page.tsx`

- [ ] **Step 1: 集成规则引擎**

1. 「预览报告」按钮点击时：
   - 收集当前表单所有字段值 → 调用 `evaluateApprovalRules`
   - 调用 `computeTriggeredApprovers`
   - 生成 `ReportItem[]` → 传给 `ApprovalReport` 组件弹窗展示

2. 「提交审批」按钮点击时：
   - 同样执行规则评估和审批人计算
   - 将报告数据附加到审批记录
   - 模拟推送钉钉（当前阶段弹 toast 提示，后续对接钉钉 API）

- [ ] **Step 2: 提交**

```bash
git add project_20260526_111004/projects/src/app/approvals/new/page.tsx
git commit -m "feat: 审批申请页集成规则引擎 — 预览报告 + 提交生成报告"
```

---

## Phase 6: 导航与收尾

### Task 6.1: 更新导航菜单

**Files:**
- Modify: `project_20260526_111004/projects/src/lib/navigation.tsx`

- [ ] **Step 1: 添加审批字段配置菜单项**

在风控审批子菜单中「自动规则」之后追加：

```typescript
{ label: '字段配置', href: '/approval/fields', icon: <NavIcons.Settings /> },
```

- [ ] **Step 2: 提交**

```bash
git add project_20260526_111004/projects/src/lib/navigation.tsx
git commit -m "feat: 导航新增审批字段配置入口"
```

---

### Task 6.2: 种子数据入库

**Files:**
- Modify: `project_20260526_111004/projects/src/db/seed.ts`

- [ ] **Step 1: 在 seed 脚本中添加审批字段初始化**

```typescript
// 插入初始审批字段
for (const field of initialApprovalFields) {
  db.insert(approvalFields).values({
    id: field.id,
    name: field.name,
    fieldKey: field.fieldKey,
    fieldType: field.fieldType,
    serviceProducts: JSON.stringify(field.serviceProducts),
    options: JSON.stringify(field.options),
    isRequired: field.isRequired ? 1 : 0,
    approvalPoint: field.approvalPoint ?? null,
    status: field.status,
    remark: field.remark ?? null,
    createdBy: field.createdBy ?? null,
    createdAt: field.createdAt,
    updatedAt: field.updatedAt ?? null,
  }).run();
}
```

- [ ] **Step 2: 运行 seed**

```bash
cd project_20260526_111004/projects && npx tsx src/db/seed.ts
```

- [ ] **Step 3: 提交**

```bash
git add project_20260526_111004/projects/src/db/seed.ts
git commit -m "feat: seed 脚本新增审批字段初始化数据"
```

---

### Task 6.3: 全链路验证

- [ ] **Step 1: 审批字段配置**

访问 `/approval/fields` → 确认 13 个字段正确显示 → 新增一个测试字段 → 编辑 → 停用 → 重新启用

- [ ] **Step 2: 审批流模板**

访问 `/approval/workflows` → 确认 7 个模板正确显示 → 编辑运输模板 → 修改职能审批人 → 保存 → 确认更新

- [ ] **Step 3: 新建审批**

访问 `/approvals/new` → 选择服务产品"运输" → 确认动态字段正确加载（浅蓝底色）→ 填写字段 → 确认实时规则检查运作 → 选"贸易代理=是" → 确认字段黄底 + 审批流追加白沥 → 预览报告 → 提交

- [ ] **Step 4: 审批详情**

访问已提交的审批详情 → 确认审批流进度可视化（已完成/当前/待处理）→ 切换到「审批辅助报告」Tab → 确认报告内容正确

- [ ] **Step 5: 审批编辑**

访问审批编辑页 → 确认已填写字段正确回显 → 修改服务产品 → 确认动态字段区更新提示

- [ ] **Step 6: 类型检查 + 构建**

```bash
cd project_20260526_111004/projects && npx tsc --noEmit --pretty && pnpm build
```

Expected: 无类型错误，构建成功。

- [ ] **Step 7: 最终提交**

```bash
git add -A
git commit -m "chore: 全链路验证通过，风控审批升级完成"
```

---

## 实施顺序

推荐按 Phase 顺序执行：1 → 2 → 3 → 4 → 5 → 6

- **Phase 1** 是所有模块的基石（类型+表+API+Store），必须先完成
- **Phase 2** 不依赖 Phase 3-5，可在 Phase 1 后独立开发
- **Phase 3** 不依赖 Phase 2/4/5，可在 Phase 1 后独立开发
- **Phase 4** 依赖 Phase 3（使用 ApprovalFlowVisual 组件）和 Phase 5（使用规则引擎函数）
- **Phase 5** 可在 Phase 1 后独立开发（纯组件+函数）
- **Phase 6** 在所有模块完成后执行

---

## Phase 7: 验收问题修复（v1.1 追加）

> **背景**：Phase 1-6 实施完成后用户验收，发现 5 个交互/UI 问题 + 1 个服务产品遗漏。本 Phase 集中修复。

### Task 7.1: 类型系统修复 — 添加"一体化供应链"

**Files:**
- Modify: `project_20260526_111004/projects/src/lib/types.ts`
- Modify: `project_20260526_111004/projects/src/lib/ui-constants.tsx`

`ServiceProduct` 联合类型追加 `'一体化供应链'`，同步更新 `SERVICE_PRODUCT_OPTIONS` 和 `SERVICE_PRODUCT_COLORS`。

---

### Task 7.2: 种子数据补全 — 一体化供应链

**Files:**
- Modify: `project_20260526_111004/projects/src/lib/sample-data.ts`

1. `serviceProductApprovalMap` 新增 `'一体化供应链': { approver: '张洁', isCountersign: false }`
2. `initialApprovalWorkflows` 新增 aw-008 模板（6 节点）
3. `initialApprovalFields` 全部 13 个字段追加 `'一体化供应链'` 到 `serviceProducts`

---

### Task 7.3: 审批流可视化组件样式优化

**Files:**
- Modify: `project_20260526_111004/projects/src/components/ApprovalFlowVisual.tsx`

- 容器 `space-y-0` → `space-y-2`
- 连接线 `min-h-[28px]` → `min-h-[40px]`
- 圆点 `w-7 h-7` → `w-8 h-8`
- 非职能节点圆点改为白底+灰色边框，卡片改为白底
- 卡片 `pb-4` → `pb-2`

---

### Task 7.4: 审批新建页修复

**Files:**
- Modify: `project_20260526_111004/projects/src/app/approvals/new/page.tsx`

1. `SERVICE_PRODUCTS` 统一使用"仓库"（非"仓储"），追加"一体化供应链"
2. `SERVICE_APPROVERS` 补全"一体化供应链"和"其他"
3. 业务主客户/商机/开票信息 UI：灰底 → 白底 + 标准边框

---

### Task 7.5: 审批编辑页全面修复

**Files:**
- Modify: `project_20260526_111004/projects/src/app/approvals/[id]/edit/page.tsx`

核心修复：
1. 移除旧版硬编码 `approvalSteps`，改用 `ApprovalFlowVisual` + `ruleTriggeredApprovers` 实现动态联动
2. `SERVICE_PRODUCTS` / `SERVICE_APPROVERS` 与新建页对齐
3. 所有选择类字段 UI 统一白底 + 标准边框
4. `handleSave()` 实现保存逻辑（弹 toast + 跳转列表）

---

### Task 7.6: 审批详情页字段补全

**Files:**
- Modify: `project_20260526_111004/projects/src/app/approvals/[id]/page.tsx`

1. 新增"合规审核"只读区块：8 个动态字段以浅蓝底卡片展示
2. 贸易代理警告行：橙色整行提示

---

### Task 7.7: 审批列表页草稿数据

**Files:**
- Modify: `project_20260526_111004/projects/src/app/approvals/page.tsx`

1. 新增 2 条草稿 mock 记录（中芯国际、长江存储）
2. `getServiceProductColor` 补全"一体化供应链"和"其他"
3. 统一"仓储"→"仓库"

---

### Task 7.8: 种子数据入库脚本补全

**Files:**
- Modify: `project_20260526_111004/projects/src/db/seed.ts`

在 seed 脚本中新增 `approval_fields` 表 CREATE + INSERT，并增加中间 `saveDb()` 调用确保数据持久化。

---

### Task 7.9: 验证

- [x] `npx tsc --noEmit` 零错误
- [x] 8 个页面全部 200：`/approvals`、`/approvals/new`、`/approvals/1`、`/approvals/1/edit`、`/approval/fields`、`/approval/workflows`、`/approval/workflows/new`、`/approval/auto-rules`
- [x] `GET /api/approval-fields` 返回 13 条记录
- [x] `approval_fields` 表 13 条 + `approval_workflows` 表 8 条（含 aw-008）
